/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package cinema_project;

import java.sql.SQLException;
//import sample.util.DBUtil;
/**
 *
 * @author tad-t
 */
public class MoviesDAO {
    /*
    public static Movies searchMovie (String mID) throws SQLException, ClassNotFoundException {
     /* //Declare a SELECT statement
        String selectStmt = "SELECT * FROM movies WHERE ID="+mID;
        //Execute SELECT statement
        try {
            //Get ResultSet from dbExecuteQuery method
            ResultSet rsEmp = DBUtil.dbExecuteQuery(selectStmt);
            //Send ResultSet to the getEmployeeFromResultSet method and get employee object
            Employee employee = getEmployeeFromResultSet(rsEmp);
            //Return employee object
            return employee;
        } catch (SQLException e) {
            System.out.println("While searching an employee with " + mID + " id, an error occurred: " + e);
            //Return exception
            throw e;
        } 

     return Movies;
    }
*/
}
